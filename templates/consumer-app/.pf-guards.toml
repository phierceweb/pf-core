# Structural gate config — read by default by `python -m pf_core.guards`
# (bin/lint, pre-commit, CI); per-layer limits + layering apply to the app/
# tree. See docs/pf-core/guards.md.
[tool.pf_guards]
root = "app"
