# __NAME__ environment. Copy to .env (bin/setup does this for you).
#
# See pf-core's docs/config.md and docs/env.md for the full set of vars.

# DATABASE_URL=               # empty = no DB; SQLite needs no driver extra
# WEB_HOST=127.0.0.1
# WEB_PORT=8000
# LOG_LEVEL=INFO
