#!/usr/bin/env bash
# Start the __NAME__ web server (uvicorn). Honours WEB_HOST / WEB_PORT / WEB_RELOAD.
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"
exec "$ROOT/.venv/bin/python" -m app "$@"
