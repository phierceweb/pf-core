# __NAME__ environment. Copy to .env (bin/setup does this for you).
#
# pf-core reads a number of env vars (API keys, LOG_LEVEL, the resolve_* knobs);
# see pf-core's docs/config.md and docs/env.md for the full reference. Add this
# project's own settings below as you declare them on Config in src/__PKG__/config.py.

# LOG_LEVEL=INFO
