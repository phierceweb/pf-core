#!/usr/bin/env bash
# Lint: ruff + pf-core's structural build gate (file-size; layering for app/ layouts).
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"
.venv/bin/python -m ruff check src tests
.venv/bin/python -m pf_core.guards   # reads .pf-guards.toml
